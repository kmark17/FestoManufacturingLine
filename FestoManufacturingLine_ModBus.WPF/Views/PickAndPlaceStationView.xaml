<UserControl
    x:Class="FestoManufacturingLine_ModBus.WPF.Views.PickAndPlaceStationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:FestoManufacturingLine_ModBus.WPF.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FestoManufacturingLine_ModBus.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="1500"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Background="#90101010">
            <controls:PlcDetails
                Margin="10"
                IsListening="{Binding IsListening}"
                IsStationOnline="{Binding IsPickAndPlaceStationOnline}"
                SendValues="{Binding SendCommand}"
                StartRecordingValues="{Binding ListenCommand}"
                StationDescription="The assembly of the workpiece takes place in this station. The workpiece is moved to the assembly position on a conveyer belt where a vacuum gripper places a cup onto the workpiece. The conveyer then forwards the piece to the following station."
                StationModBusInputVariables="{Binding PickAndPlaceStationModBusInputVariables}"
                StationModBusOutputVariables="{Binding PickAndPlaceStationModBusOutputVariables}"
                StationName="Pick And Place Station"
                StationNumberOfInputsOutputs="Number of inputs/outputs: 8DI/7DO"
                StationReqPowerAirPressure="Required power and air pressure: 24VDC, 4-6bar"
                StopRecordingValues="{Binding StopCommand}" />
        </Grid>


        <StackPanel
            Grid.Column="1"
            Margin="30,0,0,0"
            VerticalAlignment="Center">
            <Label
                Margin="0,0,0,25"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="Pick And Place Station Process"
                FontSize="30"
                FontWeight="Bold" />

            <!--  Leaving Source here for easier designing  -->
            <MediaElement
                x:Name="stationAnimationMediaElement"
                Width="auto"
                MaxHeight="750"
                Margin="30"
                MediaEnded="stationAnimationMediaElement_MediaEnded"
                Source="/Resources/Animations/PickAndPlaceStation.wmv"
                ToolTip="Source: FESTO CIROS Software"
                UnloadedBehavior="Manual" />
        </StackPanel>
    </Grid>
</UserControl>
